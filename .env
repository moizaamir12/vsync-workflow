# ── V Sync Local Development Environment ──────────────────────
# This file is for local dev only. Never commit real secrets.

# Auth (required) — random string for signing tokens
AUTH_SECRET=vsync-local-dev-secret-change-me-in-production

# OAuth (optional) — leave blank to disable social login
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
MS_CLIENT_ID=
MS_CLIENT_SECRET=

# Email (optional) — leave blank to skip sending emails
RESEND_API_KEY=re_FUapcScm_AwykoSB6prkUjWqpt69MtdS1

# App URLs
APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3001

# CORS — allow the Next.js frontend
CORS_ORIGINS=http://localhost:3000

# Database (optional) — leave blank to default to PGlite, or set to your Postgres URL
DATABASE_URL=postgresql://vsync:vsync@localhost:5432/vsync

PORT=3001

E2E=false